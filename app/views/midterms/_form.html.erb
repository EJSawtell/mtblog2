<%= form_for(@midterm) do |f| %>
  <% if @midterm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@midterm.errors.count, "error") %> prohibited this midterm from being saved:</h2>

      <ul>
      <% @midterm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Title %><br>
    <%= f.text_field :Title %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
